`int charListLen(pair(int, pair) charList)` gets length of a char list

`bool charListIsEmpty(pair(int, pair) charList)` checks if a char list is empty

`pair(int, pair) charListSingleton(char v)` creates a singleton char list

`pair(int, pair) charListInsert(pair(int, pair) charList, int index, char v)` inserts an element into a char list

`pair(int, pair) charListConcat(pair(int, pair) charList1, pair(int, pair) charList2)` concatenates two char lists

`pair(int, pair) toCharList(char[] arr)` converts a char array to a char list

`pair(int, pair) charListTake(pair(int, pair) charList, int taking)` takes the first n elements of a char list

`pair(int, pair) charListDrop(pair(int, pair) charList, int dropping)` drops the first n elements of a char list

`char intListGet(pair(int, pair) charList, int index)` gets the element at an index of a char list

`bool charListPrint(pair(int, pair) charList)` prints a char list

`pair(int, pair) charListCopy(pair(int, pair) charList)` copies a char list

`pair(int, pair) charListReplace(pair(int, pair) charList, int index, char value)` replaces the element at an index of a char list

`pair(int, pair) charListAppend(pair(int, pair) charList, char value)` appends an element to char list

`pair(int, pair) charListReversed(pair(int, pair) charList)` reverses a char list

`pair(int, pair) charListSort(pair(int, pair) charList)` sorts a char list

`bool charListFree(pair(int, pair) charList)` frees every node in a char list

`pair(int, pair) intListRemove(pair(int, pair) charList, int index)` removes an element from a char list
