`int intListLen(pair(int, pair) intList)` gets length of an int list

`bool intListIsEmpty(pair(int, pair) intList)` checks if an int list is empty

`pair(int, pair) intListSingleton(int v)` creates a singleton int list

`pair(int, pair) intListInsert(pair(int, pair) intList, int index, int v)` inserts an element into an int list

`pair(int, pair) intListConcat(pair(int, pair) intList1, pair(int, pair) intList2)` concatenates two int lists

`pair(int, pair) toIntList(int[] arr)` converts an int array to an int list

`pair(int, pair) intListTake(pair(int, pair) intList, int taking)` takes the first n elements of an int list

`pair(int, pair) intListDrop(pair(int, pair) intList, int dropping)` drops the first n elements of an int list

`int intListGet(pair(int, pair) intList, int index)` gets the element at an index of an int list

`bool intListPrint(pair(int, pair) intList)` prints an int list

`pair(int, pair) intListCopy(pair(int, pair) intList)` copies an int list

`pair(int, pair) intListReplace(pair(int, pair) intList, int index, int value)` replaces the element at an index of an int list

`pair(int, pair) intListAppend(pair(int, pair) intList, int value)` appends an element to an int list

`pair(int, pair) intListReversed(pair(int, pair) intList)` reverses an int list

`pair(int, pair) intListSort(pair(int, pair) intList)` sorts an int list

`bool intListFree(pair(int, pair) intList)` frees every node in an int list

`pair(int, pair) intListRemove(pair(int, pair) intList, int index)` removes an element from an int list
