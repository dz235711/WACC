`int boolListLen(pair(int, pair) boolList)` gets length of a bool list

`bool boolListIsEmpty(pair(int, pair) boolList)` checks if a bool list is empty

`pair(int, pair) boolListSingleton(bool v)` creates a singleton bool list

`pair(int, pair) boolListInsert(pair(int, pair) boolList, int index, bool v)` inserts an element into a bool list

`pair(int, pair) boolListConcat(pair(int, pair) boolList1, pair(int, pair) boolList2)` concatenates two bool lists

`pair(int, pair) toBoolList(bool[] arr)` converts a bool array to a bool list

`pair(int, pair) boolListTake(pair(int, pair) boolList, int taking)` takes the first n elements of a bool list

`pair(int, pair) boolListDrop(pair(int, pair) boolList, int dropping)` drops the first n elements of a bool list

`bool intListGet(pair(int, pair) boolList, int index)` gets the element at an index of a bool list

`bool boolListPrint(pair(int, pair) boolList)` prints a bool list

`pair(int, pair) boolListCopy(pair(int, pair) boolList)` copies a bool list

`pair(int, pair) boolListReplace(pair(int, pair) boolList, int index, bool value)` replaces the element at an index of a bool list

`pair(int, pair) boolListAppend(pair(int, pair) boolList, bool value)` appends an element to bool list

`pair(int, pair) boolListReversed(pair(int, pair) boolList)` reverses a bool list

`bool boolListFree(pair(int, pair) boolList)` frees every node in a bool list

`pair(int, pair) intListRemove(pair(int, pair) boolList, int index)` removes an element from a bool list
